cache: true

language: scala

scala:
  - 2.11.7

jdk:
  - oraclejdk8

before_script:
  - sbt clean test:compile

script:
  - rm -fR `find . -path "*/target/scala-2.??"`
  - sbt clean test:compile test

notifications:
  email:
    on_success: change
    on_failure: always
