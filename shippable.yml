language: scala

scala:
  - 2.12.1

jdk:
  - oraclejdk8

build:
    pre_ci:
    pre_ci_boot:
        image_name:
        image_tag:
        pull:
        options:
    ci:
        - rm -fR native/jvm/target api/jvm/target
        - sbt clean "test:compile" test
    post_ci:
    on_success:
    on_failure:
    push:
    cache: true
